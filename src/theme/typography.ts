import { typography, fontSize, color, fontFamily } from 'styled-system';
import styled from '@emotion/styled';

export const Text = styled.h1(typography, fontSize, color);

// export const Title = () => <Text fontSize={32} />;
